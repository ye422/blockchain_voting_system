# Environment variables required for the serverless email verification + SBT workflow.
# Replace the placeholder values with the real secrets and keep this file out of version control.

VERIFIER_PK="<hex-encoded private key used by the verifier wallet>"
RPC_URL="https://<ssh>/<your testnet or Quorum RPC endpoint>"
RESEND_API_KEY="<Resend REST API key for sending verification emails>"
RESEND_FROM_EMAIL="onboarding@resend.dev"
RESEND_SENDER_NAME="Agora Verification"
SUPABASE_URL="https://<your-supabase-project>.supabase.co"
SUPABASE_SERVICE_KEY="<service role key with full table access>"
HMAC_PEPPER_V1="<current pepper key material>"
HMAC_PEPPER_V2="<optional rotated pepper material>"
ACTIVE_HMAC_PEPPER_KEY_ID="v1"
AES_RETRY_KEY="<base64-safe AES-GCM key used to encrypt retry payloads>"
EMAIL_DOMAIN_ALLOWLIST="exampleuniv.ac.kr,another-approved-domain.edu"
EMAIL_CODE_EXPIRY_MINUTES="5"
SIGNATURE_TTL_MINUTES="30"
VERIFICATION_MAX_ATTEMPTS="5"
RATE_LIMIT_WINDOW_SECONDS="300"
RATE_LIMIT_MAX_REQUESTS="5"
CHAIN_ID="1337"
CITIZEN_SBT_CONTRACT_ADDRESS="0x0000000000000000000000000000000000000000"
UPSTASH_REDIS_REST_URL="https://<optional-upstash-endpoint>"
UPSTASH_REDIS_REST_TOKEN="<optional-upstash-token>"
